#include "classes/GameController.hpp"

int main(int, char**){
    /*
    GameController& game { GameController::getInstance() };
    game.start();

    while (!game.gameOver()) {
        game.process();
        game.update();
        game.render();
    }
    */
} 
